# Database Configuration
# `EXAMPLE` is a business identifier associated with the database. It dictates the folder name in src/bo and src/handlers (e.g., src/*/example/)
# `example` is the actual name of the database.
EXAMPLE_DATABASE_URL="mysql://root:password@localhost/example"
